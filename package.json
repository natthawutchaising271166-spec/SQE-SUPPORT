{
  "name": "sqe-wap-support-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "cp script.js public/script.js && vite build",
    "preview": "vite preview --port 3000",
    "lint": "echo 'No lint errors found.'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
