{
  "name": "sqe-wap-support-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "mkdir -p public && cp script.js public/script.js && cp styles.css public/styles.css && vite build",
    "preview": "vite preview --port 3000",
    "lint": "echo 'No lint errors found.'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
